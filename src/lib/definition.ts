/** Canonical one-paragraph definition of Interlude. */

export const INTERLUDE_DEFINITION =
  "Interlude is a drop-in advertising SDK and wallets that turn the seconds an AI spends thinking into a labeled, skippable video ad called a Hold: it plays only inside AI products the Holder controls (support bots, in-app assistants, homework tools, generate screens), never inside ChatGPT, Grok, Gemini, or Claude; a skip arrow appears after five seconds; if the user does not skip, the next ad plays until the answer arrives; advertisers pay Interlude to hold that space; Interlude holds the payment, then what is left is 40% Waiter Bank, 35% Holder Bank, 25% Interlude.";

/** Canonical definition of Wait — the inventory, not the ad. */
export const WAIT_DEFINITION =
  "A Wait is the time already happening between a person’s question and the AI’s answer — the thinking well. Interlude does not create that delay; the product and the network already do. That well is the inventory. A Hold is what plays inside it.";

/** Canonical definition of Holder — who owns the well. */
export const HOLDER_DEFINITION =
  "A Holder is the owner of the chat, app, or desk where people already wait — a Palms Line thread, a bank assistant, a school tutor, a civic menu, an agency bot, a campus hall. Interlude does not live inside ChatGPT, Grok, Gemini, or Claude. It lives in products the Holder controls. When a Hold plays in that well, 35% of what Interlude held from the advertiser is what is left for Holder Bank. Claim it with Interlude.holder(\"your-app\").";


